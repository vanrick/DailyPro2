const car1 = {
    brand: "Porsche",
    getCarDescription: function(cost, year, color) {
        console.log(`This care is a ${}. The price is $${cost}. The year is ${year}. The color is ${color}.\n`);

    }
};